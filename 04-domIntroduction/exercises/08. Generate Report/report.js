function generateReport() {
    
// select the checkBoxes
// if the box is checked the data from this column must be included
// create object for every row
// object property is the name attribute of the column’s header, and the value is the text content of the cell.
// Store the result in an array and output it as a JSON string display it inside the <textarea> with id "output".

 const checkField = Array.from(document.querySelectorAll('thead tr th'));
}